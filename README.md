# Next-_Node_Prisma

if u wannt intercat with the DB
docker exec -it db psql -U postgres



npx prisma init
npx prisma generate



for create front end
npm create-next-app@latest --no --git

npm run dev
